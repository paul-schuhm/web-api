/**
 * Ouverture de la connexion à la base de données avec l'ORM Sequelize,
 * et test de la connexion.
 */
// const { Sequelize } = require('sequelize');

// const sequelize = new Sequelize('mysql://root:root@127.0.0.1:5002/mydb')

// async function testConnexion(sequelize){
//     try {
//         await sequelize.authenticate();
//         console.log('Connection has been established successfully.');
//     } catch (error) {
//         console.error('Unable to connect to the database:', error);
//     }
// }

// testConnexion(sequelize)

// module.exports = sequelize